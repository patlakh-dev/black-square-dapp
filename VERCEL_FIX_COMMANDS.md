# 🔧 Исправление команд для Vercel

## Проблема
Команда `cd frontend && npm install` падает с ошибкой.

## Решение: Изменить команды в Vercel

Попробуйте использовать команды без `cd`, но с указанием рабочей директории через `--prefix`:

### Вариант 1: Использовать --prefix (РЕКОМЕНДУЕТСЯ)

В настройках проекта Vercel измените команды:

1. **Build Command:**
   ```
   npm install --prefix frontend && npm run build --prefix frontend
   ```

2. **Output Directory:**
   ```
   frontend/dist
   ```

3. **Install Command:**
   ```
   npm install --prefix frontend
   ```

### Вариант 2: Использовать полный путь

1. **Build Command:**
   ```
   (cd frontend && npm install && npm run build)
   ```

2. **Output Directory:**
   ```
   frontend/dist
   ```

3. **Install Command:**
   ```
   (cd frontend && npm install)
   ```

### Вариант 3: Использовать shell команду явно

1. **Build Command:**
   ```
   sh -c "cd frontend && npm install && npm run build"
   ```

2. **Output Directory:**
   ```
   frontend/dist
   ```

3. **Install Command:**
   ```
   sh -c "cd frontend && npm install"
   ```

## Где изменить команды в Vercel

1. Откройте ваш проект в Vercel
2. Перейдите в **Settings** → **General**
3. Найдите секцию **"Build & Development Settings"** или **"Build Command"**
4. Нажмите на иконку карандаша (✏️) рядом с командами
5. Измените команды на один из вариантов выше
6. Сохраните
7. Запустите новый деплой (Redeploy)

## Проверка

После изменения команд проверьте логи деплоя - команды должны выполниться успешно.

