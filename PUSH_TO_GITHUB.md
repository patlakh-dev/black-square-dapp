# 🚀 Инструкция: Отправка кода на GitHub

Ваш код готов к отправке! Осталось только авторизоваться.

## Вариант 1: Использование Personal Access Token (Рекомендуется)

### Шаг 1: Создайте Personal Access Token

1. Перейдите на GitHub → **Settings** (правый верхний угол) → **Developer settings**
2. Выберите **Personal access tokens** → **Tokens (classic)**
3. Нажмите **"Generate new token (classic)"**
4. Заполните:
   - **Note:** `Git Push Token`
   - **Expiration:** выберите срок (например, 90 дней)
   - **Scopes:** отметьте галочку **`repo`** (полный доступ к репозиториям)
5. Нажмите **"Generate token"**
6. **⚠️ ВАЖНО:** Скопируйте токен сразу! Он больше не будет показан. Выглядит примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 2: Выполните push

Откройте терминал и выполните:

```bash
cd "/Users/daniel/black square"
git push -u origin main
```

**Когда Git попросит:**
- **Username:** введите `patlakh-dev`
- **Password:** вставьте ваш **Personal Access Token** (не обычный пароль!)

---

## Вариант 2: Использование SSH (Альтернатива)

Если хотите использовать SSH вместо HTTPS:

### Шаг 1: Проверьте наличие SSH ключа

```bash
ls -la ~/.ssh
```

Если файлов `id_rsa` и `id_rsa.pub` нет, создайте их:

```bash
ssh-keygen -t ed25519 -C "dpatlakhh@gmail.com"
# Нажмите Enter для всех вопросов
```

### Шаг 2: Добавьте SSH ключ на GitHub

1. Скопируйте публичный ключ:
```bash
cat ~/.ssh/id_ed25519.pub
```

2. GitHub → **Settings** → **SSH and GPG keys** → **New SSH key**
3. Вставьте скопированный ключ
4. Сохраните

### Шаг 3: Измените remote на SSH

```bash
cd "/Users/daniel/black square"
git remote set-url origin git@github.com:patlakh-dev/black-square-dapp.git
git push -u origin main
```

---

## ✅ После успешного push

Откройте https://github.com/patlakh-dev/black-square-dapp в браузере - вы должны увидеть все файлы проекта!

---

## 🆘 Если возникли проблемы

**Ошибка "Authentication failed":**
- Убедитесь, что используете Personal Access Token, а не пароль
- Проверьте, что токен имеет права `repo`

**Ошибка "Permission denied":**
- Проверьте, что репозиторий существует и вы имеете к нему доступ
- Убедитесь, что используете правильный username (`patlakh-dev`)

**Ошибка "Repository not found":**
- Проверьте URL репозитория: https://github.com/patlakh-dev/black-square-dapp
- Убедитесь, что репозиторий публичный или у вас есть доступ

---

**После успешного push переходите к деплою на Vercel/Netlify!** 🎉

