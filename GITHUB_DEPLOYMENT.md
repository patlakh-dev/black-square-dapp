# 📚 Подробная инструкция: GitHub + Vercel/Netlify

Эта инструкция поможет вам загрузить проект на GitHub и задеплоить его на Vercel или Netlify, чтобы сайт был доступен всем в интернете.

---

## 🎯 Часть 1: Загрузка проекта на GitHub

### Шаг 1: Создание аккаунта и репозитория на GitHub

1. **Если у вас еще нет аккаунта GitHub:**
   - Перейдите на https://github.com
   - Нажмите "Sign up" (Зарегистрироваться)
   - Заполните форму и создайте аккаунт
   - Подтвердите email

2. **Создайте новый репозиторий:**
   - Войдите в GitHub
   - Нажмите на "+" в правом верхнем углу → "New repository"
   - **Repository name:** `black-square-dapp` (или любое другое название)
   - **Description:** `Suprematism-inspired Web3 dApp with NFT staking`
   - Выберите **Public** (публичный)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть эти файлы)
   - Нажмите "Create repository"

3. **Скопируйте URL репозитория:**
   - После создания репозитория GitHub покажет страницу с инструкциями
   - Скопируйте URL (например: `https://github.com/ваш-username/black-square-dapp.git`)
   - Он понадобится в следующем шаге

---

### Шаг 2: Настройка Git на вашем компьютере

**Если Git еще не установлен:**
- Скачайте с https://git-scm.com/downloads
- Установите, следуя инструкциям
- Перезапустите терминал

**Проверьте установку:**
```bash
git --version
```

**Настройте Git (если еще не настроен):**
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

**✅ Уже настроено автоматически:**
- Имя: Daniil Patlakh
- Email: dpatlakhh@gmail.com

---

### Шаг 3: Инициализация Git в проекте

Откройте терминал в папке проекта и выполните:

```bash
cd "/Users/daniel/black square"
git init
```

**✅ Уже выполнено автоматически!** Git репозиторий инициализирован.

---

### Шаг 4: Добавление файлов в Git

**Проверьте, что вы в правильной папке:**
```bash
pwd
# Должно показать: /Users/daniel/black square
```

**Добавьте все файлы:**
```bash
git add .
```

**Проверьте, что добавлено:**
```bash
git status
```

Вы должны увидеть список файлов, которые будут загружены. Убедитесь, что **НЕТ** файлов `.env` в списке (они должны быть в `.gitignore`).

---

### Шаг 5: Первый коммит

```bash
git commit -m "Initial commit: Black Square dApp with NFT staking"
```

**Что такое коммит?** Это "снимок" вашего кода в определенный момент времени. Сообщение коммита описывает, что было сделано.

---

### Шаг 6: Подключение к GitHub и загрузка

**Подключите ваш локальный репозиторий к GitHub:**
```bash
git remote add origin https://github.com/ваш-username/black-square-dapp.git
```

**Замените `ваш-username` и `black-square-dapp` на ваши реальные значения!**

**Загрузите код на GitHub:**
```bash
git branch -M main
git push -u origin main
```

**Если GitHub попросит авторизацию:**
- Введите ваш GitHub username
- Для пароля используйте **Personal Access Token** (не обычный пароль)

**Как создать Personal Access Token:**
1. GitHub → Settings (правый верхний угол) → Developer settings → Personal access tokens → Tokens (classic)
2. "Generate new token (classic)"
3. Название: `Git Push Token`
4. Выберите срок действия (например, 90 дней)
5. Отметьте галочку `repo` (полный доступ к репозиториям)
6. Нажмите "Generate token"
7. **Скопируйте токен сразу** (он больше не будет показан!)
8. Используйте этот токен как пароль при `git push`

---

### Шаг 7: Проверка

Откройте ваш репозиторий на GitHub в браузере. Вы должны увидеть все файлы проекта!

---

## 🚀 Часть 2: Деплой на Vercel

### Шаг 1: Регистрация на Vercel

1. Перейдите на https://vercel.com
2. Нажмите "Sign Up"
3. Выберите "Continue with GitHub"
4. Авторизуйтесь через GitHub
5. Разрешите Vercel доступ к вашим репозиториям

---

### Шаг 2: Импорт проекта

1. В Vercel нажмите "Add New..." → "Project"
2. Найдите ваш репозиторий `black-square-dapp` в списке
3. Нажмите "Import"

---

### Шаг 3: Настройка проекта

Vercel автоматически определит настройки, но проверьте:

- **Framework Preset:** Vite
- **Root Directory:** `frontend` (или оставьте пустым, если проект в корне)
- **Build Command:** `cd frontend && npm install && npm run build`
- **Output Directory:** `frontend/dist`
- **Install Command:** `cd frontend && npm install`

**Если Root Directory пустой, измените:**
- **Root Directory:** `frontend`

---

### Шаг 4: Добавление переменных окружения

**Перед деплоем добавьте переменные окружения:**

1. В разделе "Environment Variables" нажмите "Add"
2. Добавьте каждую переменную:

```
VITE_WALLETCONNECT_PROJECT_ID = ваш_project_id
VITE_BLACK_SQUARE_CONTRACT_ADDRESS = 0x...
VITE_NFT_STAKING_ADDRESS = 0x...
VITE_MALEVICH_COLLECTION_ADDRESS = 0x...
```

**Где взять значения:**
- `VITE_WALLETCONNECT_PROJECT_ID`: https://cloud.walletconnect.com → создайте проект → скопируйте Project ID
- Адреса контрактов: из вашего `.env` файла в папке `frontend`

3. Нажмите "Deploy"

---

### Шаг 5: Ожидание деплоя

Vercel начнет сборку проекта. Это займет 2-5 минут.

**Что происходит:**
- Vercel клонирует ваш репозиторий
- Устанавливает зависимости (`npm install`)
- Собирает проект (`npm run build`)
- Деплоит на CDN

---

### Шаг 6: Получение URL

После успешного деплоя вы получите URL вида:
```
https://black-square-dapp.vercel.app
```

Или можете настроить свой домен в настройках проекта.

---

## 🌐 Часть 3: Деплой на Netlify

### Шаг 1: Регистрация на Netlify

1. Перейдите на https://netlify.com
2. Нажмите "Sign up"
3. Выберите "Sign up with GitHub"
4. Авторизуйтесь и разрешите доступ

---

### Шаг 2: Импорт проекта

1. Нажмите "Add new site" → "Import an existing project"
2. Выберите "Deploy with GitHub"
3. Найдите ваш репозиторий `black-square-dapp`
4. Нажмите "Configure"

---

### Шаг 3: Настройка сборки

Netlify автоматически определит настройки из `netlify.toml`, но проверьте:

- **Base directory:** `frontend`
- **Build command:** `npm install && npm run build`
- **Publish directory:** `frontend/dist`

---

### Шаг 4: Добавление переменных окружения

1. Перед деплоем нажмите "Show advanced"
2. В разделе "Environment variables" добавьте:

```
VITE_WALLETCONNECT_PROJECT_ID = ваш_project_id
VITE_BLACK_SQUARE_CONTRACT_ADDRESS = 0x...
VITE_NFT_STAKING_ADDRESS = 0x...
VITE_MALEVICH_COLLECTION_ADDRESS = 0x...
```

3. Нажмите "Deploy site"

---

### Шаг 5: Ожидание деплоя

Netlify начнет сборку. Процесс займет 2-5 минут.

---

### Шаг 6: Получение URL

После деплоя вы получите URL вида:
```
https://random-name-123456.netlify.app
```

Или можете настроить свой домен в "Site settings" → "Domain management".

---

## ✅ Проверка после деплоя

1. Откройте ваш сайт по полученному URL
2. Проверьте, что:
   - Страница загружается
   - Можно подключить кошелек
   - Отображаются правильные адреса контрактов
   - NFT стейкинг работает

---

## 🔄 Обновление сайта

**После любых изменений в коде:**

1. Внесите изменения в файлы
2. Выполните в терминале:
```bash
git add .
git commit -m "Описание изменений"
git push
```

3. **Vercel/Netlify автоматически пересоберут и задеплоят сайт!** (если подключен GitHub)

---

## 🆘 Решение проблем

### Ошибка при `git push`:
- Проверьте, что используете Personal Access Token, а не пароль
- Убедитесь, что репозиторий существует на GitHub

### Сайт не работает после деплоя:
- Проверьте, что все переменные окружения добавлены
- Проверьте логи сборки в Vercel/Netlify
- Убедитесь, что контракты задеплоены на Sepolia

### Переменные окружения не работают:
- В Vercel/Netlify переменные должны начинаться с `VITE_`
- После добавления переменных пересоберите проект (Redeploy)

---

## 📝 Полезные команды Git

```bash
# Проверить статус файлов
git status

# Посмотреть историю коммитов
git log

# Отменить изменения в файле (если еще не закоммитили)
git checkout -- имя_файла

# Обновить локальную копию с GitHub
git pull
```

---

**Готово!** Теперь ваш сайт доступен всем в интернете! 🎉

