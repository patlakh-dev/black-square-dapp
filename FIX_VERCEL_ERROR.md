# 🔧 Исправление ошибки "cd: frontend: No such file or directory"

## Проблема
Vercel не может найти папку `frontend` потому что Root Directory не установлен правильно.

## Решение

### Вариант 1: Установить Root Directory (РЕКОМЕНДУЕТСЯ)

1. В Vercel на странице настройки проекта найдите поле **"Root Directory"**
   - Оно может быть выше секции "Build and Output Settings"
   - Или в настройках проекта (Settings → General)

2. Установите Root Directory в: `frontend`

3. Измените команды (нажмите на иконку карандаша ✏️):
   - **Build Command:** `npm install && npm run build` (БЕЗ `cd frontend &&`)
   - **Output Directory:** `dist` (БЕЗ `frontend/`)
   - **Install Command:** `npm install` (БЕЗ `cd frontend &&`)

4. Сохраните и попробуйте деплой снова

### Вариант 2: Оставить Root Directory пустым

Если не можете найти поле Root Directory:

1. Измените команды (нажмите на иконку карандаша ✏️):
   - **Build Command:** `cd frontend && npm install && npm run build`
   - **Output Directory:** `frontend/dist`
   - **Install Command:** `cd frontend && npm install`

2. Убедитесь, что Root Directory пустое (не установлен)

3. Сохраните и попробуйте деплой снова

## Где найти Root Directory в Vercel

1. Откройте ваш проект в Vercel
2. Перейдите в **Settings** (настройки)
3. Выберите **General** (Общие)
4. Найдите **Root Directory**
5. Установите: `frontend`
6. Сохраните изменения

## После исправления

После изменения настроек:
1. Перейдите на вкладку **Deployments**
2. Нажмите на последний деплой (который упал с ошибкой)
3. Нажмите **"Redeploy"** или создайте новый деплой

