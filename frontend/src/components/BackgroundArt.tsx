/**
 * Suprematist-inspired animated background art
 * Minimal geometric shapes with subtle animations
 */
export function BackgroundArt() {
  return (
    <div className="fixed inset-0 pointer-events-none overflow-hidden z-0">
      {/* Background art removed - squares that scroll with mouse */}
    </div>
  );
}

