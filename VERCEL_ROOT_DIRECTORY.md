# 🔧 Как установить Root Directory в Vercel

## Проблема
Vercel не может найти папку `frontend` потому что Root Directory не установлен.

## Решение: Установить Root Directory через веб-интерфейс

Root Directory **НЕ** настраивается через `vercel.json` - его нужно установить в веб-интерфейсе Vercel.

### Способ 1: Через Build and Deployment (РЕКОМЕНДУЕТСЯ)

1. Откройте ваш проект в Vercel Dashboard
2. Перейдите на вкладку **Settings** (вверху)
3. Выберите **Build and Deployment** (слева в меню, второй пункт)
4. Прокрутите вниз до секции **"Root Directory"**
5. Если поле пустое или показывает что-то другое:
   - Нажмите на поле или кнопку **"Edit"** / **"Change"**
   - Введите: `frontend`
   - Нажмите **"Save"**

### Способ 2: Через General (если не нашли в Build and Deployment)

1. Откройте ваш проект в Vercel Dashboard
2. Перейдите на вкладку **Settings** (вверху)
3. Выберите **General** (слева в меню, первый пункт)
4. Прокрутите вниз до секции **"Root Directory"** или **"Build & Development Settings"**
5. Если поле пустое:
   - Нажмите на поле или кнопку **"Edit"**
   - Введите: `frontend`
   - Нажмите **"Save"**

### Способ 2: При импорте проекта (если еще не задеплоили)

1. На странице "Configure Project" (при импорте)
2. Найдите поле **"Root Directory"** (может быть вверху страницы)
3. Установите: `frontend`

### Способ 3: Если Root Directory не видно

Если вы не видите поле Root Directory:

1. **В Settings → General:**
   - Прокрутите страницу вниз
   - Ищите секцию "Build & Development Settings"
   - Там должно быть поле Root Directory

2. **Или используйте команды с `cd frontend`:**
   - Оставьте Root Directory пустым
   - Убедитесь, что команды содержат `cd frontend &&`:
     - Build Command: `cd frontend && npm install && npm run build`
     - Output Directory: `frontend/dist`
     - Install Command: `cd frontend && npm install`

## После установки Root Directory

После установки Root Directory в `frontend`:

1. Измените команды (убрав `cd frontend &&`):
   - **Build Command:** `npm install && npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

2. Сохраните изменения

3. Запустите новый деплой (Redeploy)

## Проверка

После установки Root Directory и изменения команд, новый деплой должен пройти успешно без ошибки "No such file or directory".

